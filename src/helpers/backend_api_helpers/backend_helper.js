// import { APIClient } from "./api_helpers";
// import * as url from "../url_helper";

// const api = new APIClient();
// export const getAllConversations = async () => {
//   return await api.create(url.GET_CONVERSATIONS);
// };

// export const getMessagesById = async (id) => {
//   return await api.create(url.GET_MESSAGES + "/" + id, { params: { id } });
// };

// export const getConversationByTag = async (tag) => {
//   return await api.create(url.GET_CONVERSATIONS + "/" + tag, {
//     params: { tag },
//   });
// };
